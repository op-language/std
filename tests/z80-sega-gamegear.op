//! Test: z80-sega-gamegear
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet z80-sega-gamegear.

use std::*;

#[cfg(cpu = "z80")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "sega")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "gamegear")]
const _TEST_MACHINE: u8 = 0;

#[cfg(target = "z80-sega-gamegear")]
const _TEST_TARGET: u8 = 0;
