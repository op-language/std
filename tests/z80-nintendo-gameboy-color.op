//! Test: z80-nintendo-gameboy-color
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet z80-nintendo-gameboy-color.

use std::*;

#[cfg(cpu = "z80")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "nintendo")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "gameboy-color")]
const _TEST_MACHINE: u8 = 0;

#[cfg(target = "z80-nintendo-gameboy-color")]
const _TEST_TARGET: u8 = 0;
