//! Test: m68000-neogeo-aes
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet m68000-neogeo-aes.

use std::*;

#[cfg(cpu = "m68000")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "neogeo")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "aes")]
const _TEST_MACHINE: u8 = 0;

#[cfg(target = "m68000-neogeo-aes")]
const _TEST_TARGET: u8 = 0;
