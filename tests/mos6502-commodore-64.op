//! Test: mos6502-commodore-64
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet mos6502-commodore-64.

use std::*;

#[cfg(cpu = "mos6502")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "commodore")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "64")]
const _TEST_MACHINE: u8 = 0;

#[cfg(target = "mos6502-commodore-64")]
const _TEST_TARGET: u8 = 0;
