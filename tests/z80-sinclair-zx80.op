//! Test: z80-sinclair-zx80
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet z80-sinclair-zx80.

use std::*;

#[cfg(cpu = "z80")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "sinclair")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "zx80")]
const _TEST_MACHINE: u8 = 0;

#[cfg(target = "z80-sinclair-zx80")]
const _TEST_TARGET: u8 = 0;
