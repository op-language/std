//! Test: mos6502-atari-800-pal
//!
//! Verifies that the cfg predicates for cpu, manufacturer, machine,
//! variant, and target match the triplet mos6502-atari-800-pal.

use std::*;

#[cfg(cpu = "mos6502")]
const _TEST_CPU: u8 = 0;

#[cfg(manufacturer = "atari")]
const _TEST_MFR: u8 = 0;

#[cfg(machine = "800")]
const _TEST_MACHINE: u8 = 0;

#[cfg(variant = "pal")]
const _TEST_VARIANT: u8 = 0;

#[cfg(target = "mos6502-atari-800-pal")]
const _TEST_TARGET: u8 = 0;
